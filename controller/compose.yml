services:
  relay-alpha:
    build:
      context: .
      dockerfile: Containerfile
    image: localhost/zrp-controller:latest
    network_mode: "host"
    environment:
      - PORT=9000

  relay-beta:
    image: localhost/zrp-controller:latest
    network_mode: "host"
    environment:
      - PORT=9001
